#include <iostream>
#include "LinearRegression.h"

int main(int argc, char* argv[]) {
	/*
	double** dataSet;
	dataSet = two_dimensional_allocation(5, 2);
	
	for (int i = 0; i < 5; i++) {
		dataSet[i][0] = static_cast<double>(i) + 1;
	}

	dataSet[0][1] = 50.0;
	dataSet[1][1] = 55.0;
	dataSet[2][1] = 65.0;
	dataSet[3][1] = 70.0;
	dataSet[4][1] = 75.0;
	
	double x_mean = xMean(dataSet, 5);
	double y_mean = yMean(dataSet, 5);

	double gradient = summation_mean_beta_one(dataSet, 5, x_mean, y_mean);
	double y_intercept = y_intercept_beta_zero(y_mean, x_mean, gradient);
	
	double prediction = linear_regression_prediction(gradient, y_intercept, 6.0);
	printf("X-Mean = %f, Y-Mean = %f\nGradient = %f\nY-Intercept = %f\nY prediction if x = 6.0? -> %f\n", x_mean, y_mean, gradient, y_intercept, prediction);
	
	free_two_dimensional_array(dataSet, 5, 2);
	getchar(); 
	return 0;
	*/

	menu();
}
